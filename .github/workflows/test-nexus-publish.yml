name: Test Nexus Publish

on:
  workflow_dispatch:
    inputs:
      modules:
        description: 'Modules to publish (comma-separated, e.g., cubicolor-core,cubicolor-theme). Leave empty for all modules.'
        required: false
        type: string
        default: ''

jobs:
  build-and-test:
    runs-on: arc-runner-set

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Get current version
        id: version
        run: |
          VERSION=$(./gradlew currentVersion -q | grep "Project version:" | awk '{print $3}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $VERSION"

      - name: Test Nexus Publish
        env:
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
          NEXUS_RELEASE_URL: ${{ secrets.NEXUS_RELEASE_URL }}
          NEXUS_SNAPSHOT_URL: ${{ secrets.NEXUS_SNAPSHOT_URL }}
        run: |
          set -e

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🧪 Testing Nexus Publish"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🏷️  Version: ${{ steps.version.outputs.version }}"

          if [ -z "${{ inputs.modules }}" ]; then
            echo "🎯 Testing all modules"
            ./gradlew publish --no-daemon --stacktrace
          else
            echo "🎯 Testing selected modules: ${{ inputs.modules }}"
            IFS=',' read -ra MODULES <<< "${{ inputs.modules }}"
            for module in "${MODULES[@]}"; do
              module=$(echo "$module" | xargs)
              echo "📦 Publishing ${module}..."
              ./gradlew :${module}:publish --no-daemon --stacktrace
            done
          fi

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Test publish complete!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
