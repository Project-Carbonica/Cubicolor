name: Nexus Publish

on:
  workflow_call:
    inputs:
      version:
        description: 'Version to publish (e.g., 1.0.0)'
        required: true
        type: string
      artifactName:
        description: 'Name of the GitHub artifact to download'
        required: true
        type: string
      groupId:
        description: 'Maven groupId (e.g., com.example.myapp)'
        required: true
        type: string
      projectName:
        description: 'Project name for POM'
        required: false
        type: string
        default: ''
      projectDescription:
        description: 'Project description for POM'
        required: false
        type: string
        default: ''
      projectUrl:
        description: 'Project URL for POM'
        required: false
        type: string
        default: ''
      licenseName:
        description: 'License name (e.g., MIT License)'
        required: false
        type: string
        default: 'MIT License'
      licenseUrl:
        description: 'License URL'
        required: false
        type: string
        default: 'https://opensource.org/licenses/MIT'
    secrets:
      NEXUS_USERNAME:
        required: true
      NEXUS_PASSWORD:
        required: true
      NEXUS_RELEASE_URL:
        required: true
      NEXUS_SNAPSHOT_URL:
        required: true

jobs:
  publish:
    runs-on: arc-runner-set

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.artifactName }}
          path: artifacts

      - name: List downloaded artifacts
        run: |
          echo "📦 Downloaded artifacts:"
          find artifacts -type f -name "*.jar" | grep -v -- '-plain.jar' || echo "No JAR files found"

      - name: Publish to Nexus
        env:
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
          NEXUS_RELEASE_URL: ${{ secrets.NEXUS_RELEASE_URL }}
          NEXUS_SNAPSHOT_URL: ${{ secrets.NEXUS_SNAPSHOT_URL }}
          VERSION: ${{ inputs.version }}
          GROUP_ID: ${{ inputs.groupId }}
          PROJECT_NAME: ${{ inputs.projectName }}
          PROJECT_DESCRIPTION: ${{ inputs.projectDescription }}
          PROJECT_URL: ${{ inputs.projectUrl }}
          LICENSE_NAME: ${{ inputs.licenseName }}
          LICENSE_URL: ${{ inputs.licenseUrl }}
        run: |
          set -e

          # Convert groupId to path (com.example.app -> com/example/app)
          GROUP_PATH="${GROUP_ID//./\/}"

          # Determine repository URL based on version
          if [[ "$VERSION" == *"SNAPSHOT"* ]]; then
            REPO_URL="${NEXUS_SNAPSHOT_URL}"
            echo "📍 Publishing SNAPSHOT version to: ${REPO_URL}"
          else
            REPO_URL="${NEXUS_RELEASE_URL}"
            echo "📍 Publishing RELEASE version to: ${REPO_URL}"
          fi

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🚀 Nexus Publish Configuration"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Group ID: ${GROUP_ID}"
          echo "🏷️  Version: ${VERSION}"
          echo "📍 Repository: ${REPO_URL}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          # Function to generate POM
          generate_pom() {
            local ARTIFACT_ID=$1
            local POM_PATH=$2

            cat > "$POM_PATH" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <project xmlns="http://maven.apache.org/POM/4.0.0"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                   http://maven.apache.org/xsd/maven-4.0.0.xsd">
            <modelVersion>4.0.0</modelVersion>
            <groupId>${GROUP_ID}</groupId>
            <artifactId>${ARTIFACT_ID}</artifactId>
            <version>${VERSION}</version>
          EOF

            # Add optional fields if provided
            if [ -n "$PROJECT_NAME" ]; then
              echo "  <name>${PROJECT_NAME} - ${ARTIFACT_ID}</name>" >> "$POM_PATH"
            fi

            if [ -n "$PROJECT_DESCRIPTION" ]; then
              echo "  <description>${PROJECT_DESCRIPTION}</description>" >> "$POM_PATH"
            fi

            if [ -n "$PROJECT_URL" ]; then
              echo "  <url>${PROJECT_URL}</url>" >> "$POM_PATH"
            fi

            # Add license
            cat >> "$POM_PATH" <<EOF
            <licenses>
              <license>
                <name>${LICENSE_NAME}</name>
                <url>${LICENSE_URL}</url>
              </license>
            </licenses>
          </project>
          EOF
          }

          # Function to upload artifact
          upload_artifact() {
            local JAR_PATH=$1
            local ARTIFACT_ID=$(basename "$JAR_PATH" | sed "s/-${VERSION}.jar//")

            if [ ! -f "$JAR_PATH" ]; then
              echo "⚠️  Warning: $JAR_PATH not found, skipping..."
              return
            fi

            # Skip plain JARs (Spring Boot generates these)
            if [[ "$JAR_PATH" == *"-plain.jar" ]]; then
              echo "⏭️  Skipping plain JAR: $ARTIFACT_ID"
              return
            fi

            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "📦 Processing: ${ARTIFACT_ID}"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

            # Maven path structure
            local MAVEN_BASE="${GROUP_PATH}/${ARTIFACT_ID}/${VERSION}"
            local JAR_UPLOAD_URL="${REPO_URL}${MAVEN_BASE}/${ARTIFACT_ID}-${VERSION}.jar"
            local POM_UPLOAD_URL="${REPO_URL}${MAVEN_BASE}/${ARTIFACT_ID}-${VERSION}.pom"

            echo "   📂 Artifact ID: ${ARTIFACT_ID}"
            echo "   📁 Source: ${JAR_PATH}"
            echo "   🌐 Target: ${JAR_UPLOAD_URL}"
            echo ""

            # Upload JAR
            echo "   📤 Uploading JAR..."
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
              -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" \
              --upload-file "${JAR_PATH}" \
              "${JAR_UPLOAD_URL}")

            if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
              echo "   ✅ JAR uploaded successfully (HTTP ${HTTP_CODE})"
            else
              echo "   ❌ JAR upload failed (HTTP ${HTTP_CODE})"
              exit 1
            fi

            # Generate and upload POM
            local POM_PATH="/tmp/${ARTIFACT_ID}-${VERSION}.pom"
            generate_pom "$ARTIFACT_ID" "$POM_PATH"

            echo "   📤 Uploading POM..."
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
              -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" \
              --upload-file "${POM_PATH}" \
              "${POM_UPLOAD_URL}")

            if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
              echo "   ✅ POM uploaded successfully (HTTP ${HTTP_CODE})"
            else
              echo "   ⚠️  POM upload warning (HTTP ${HTTP_CODE})"
            fi

            rm -f "$POM_PATH"
          }

          # Find and upload all JARs
          echo "🔍 Searching for artifacts..."
          echo ""

          UPLOADED=0
          while IFS= read -r jar_file; do
            upload_artifact "$jar_file"
            UPLOADED=$((UPLOADED + 1))
          done < <(find artifacts -type f -name "*.jar" | grep -v -- '-plain.jar')

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✨ Publishing Complete!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Total artifacts published: ${UPLOADED}"
          echo "📍 Repository: ${REPO_URL}"
          echo "🏷️  Version: ${VERSION}"
          echo ""
          echo "📋 Maven coordinates format:"
          echo "   <dependency>"
          echo "     <groupId>${GROUP_ID}</groupId>"
          echo "     <artifactId>YOUR-ARTIFACT-ID</artifactId>"
          echo "     <version>${VERSION}</version>"
          echo "   </dependency>"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
